worker_processes  1;

events {
  worker_connections  1024;
}

http {
  server {
    listen 12006 default_server;
    listen [::]:12006 default_server;

    root ./static;

    location / {
      try_files $uri $uri.sh $uri/ =404;
    }
  }
}
